// ipcl = String(ip)
// let ipban = ["211.117.66.58"]
// alert(ipcl)
// alert(ipban.includes(ipcl))
// alert(ipban)
// if (ipban.includes(ipcl)) {
	// alert("이 IP(" + ipcl+ ")는 차단되었으므로 접속할 수 없습니다.")
	// history.back()
// }
